<nav class='color-secondary border-gray-200 px-4 drop-shadow-xl py-2.5 mb-10 rounded'>
  <div class='container flex flex-wrap items-center justify-between px-3'>
    <a href='/dashboard' class='flex items-center'>
      <span class='text-rose-700 self-center text-xl text-secondary font-semibold whitespace-nowrape'>
        E-Wallet
      </span>
    </a>
    <div class='flex md:order-2'>
      <div data-controller="dropdown" class="relative">
        <button class="text-primary" type="button" data-action="dropdown#toggle click@window->dropdown#hide">
          <div class="flex items-center">
            <%if current_user.profile_pic.attached?%>
              <p class="mx-1 "><%= cl_image_tag(current_user.profile_pic.key, :width => 50, :crop => :thumb) %></p>
            <%end%>
            <%= current_user.name.humanize%>
            <i class="fa-solid fa-chevron-down ml-2"></i>
          </div>
        </button>
        <div
          data-dropdown-target="menu"
          class="hidden transition transform origin-top-right absolute right-0 bg-white p-4 w-40"
          data-transition-enter-from="opacity-0 scale-95"
          data-transition-enter-to="opacity-100 scale-100"
          data-transition-leave-from="opacity-100 scale-100"
          data-transition-leave-to="opacity-0 scale-95"
          >
          <ul class="space-y-3">
            <li><%= link_to "View Profile", user_path(current_user), data: {action: "dropdown#toggle"}%></li>
            <li><%= link_to "Edit Profile", edit_user_registration_path(current_user), data: {action: "dropdown#toggle"}%></li>
            <hr/>
            <li><%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete}, class: "text-primary"%></li>
          </ul>
        </div>
      </div>
    </div>
    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
      <ul class="flex flex-col p-4 mt-4 border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 md:mt-0 md:text-sm md:font-medium md:border-0 md:bg-white dark:bg-gray-800 md:dark:bg-gray-900 dark:border-gray-700">
        <li>
          <%= link_to "Home", dashboard_index_path, class: 'text-primary font-semibold hover:text-rose-400'%>
        </li>
        <li>
          <%= link_to "Payee", friendships_path, class: 'text-primary font-semibold hover:text-rose-400'%>
        </li>
        <li>
          <%= link_to "Transaction", new_fund_transaction_path , class: 'text-primary font-semibold hover:text-rose-400'%>
        </li>
        <li>
          <%= link_to "Account Statement", fund_transactions_path , class: 'text-primary font-semibold hover:text-rose-400'%>
        </li>
      </ul>
    </div>
  </div>
</nav>
